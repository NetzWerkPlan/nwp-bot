apiVersion: v1
kind: Service
metadata:
  name: {{ include "nwp-bot.fullname" . }}
  labels:
    app: {{ include "nwp-bot.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: {{ include "nwp-bot.name" . }}
